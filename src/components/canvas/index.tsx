import { h } from "preact";
// import * as style from "./style.css";

interface Props {}

const Canvas: preact.FunctionalComponent<Props> = props => {
    return (
        <div>
        <canvas>
        </canvas>
        </div>
    );
};

export default Canvas;

